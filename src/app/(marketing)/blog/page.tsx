import { BlogShowcase } from "@/components/blog/blog-showcase";

export const metadata = {
  title: "Blog",
  description: "Insights on AI-assisted real estate search and market positioning.",
};

export default function BlogPage() {
  return <BlogShowcase />;
}